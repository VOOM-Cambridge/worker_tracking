[constants]
serialID = '16c0:27db'
location = 'Manual_Lab' 
brokerIP = 'mqtt.docker.local'

[docker]
    url = "http://timeseries-db.docker.local:1883"
    frequency_record_seconds = 1
    ## Token for authentication.

[sqlite3]
    dataBaseName = 'WORKER'
    filePath = 'data/workerData.db'
